interface:
  display_name: "TBA API"
  short_description: "Work with FRC data from The Blue Alliance"
  default_prompt: "Use $tba-api to fetch, normalize, and analyze FRC event data with tbapy."

policy:
  allow_implicit_invocation: true
