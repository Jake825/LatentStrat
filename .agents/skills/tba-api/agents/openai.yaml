interface:
  display_name: "TBA API"
  short_description: "Retrieve FRC data from The Blue Alliance"
  default_prompt: "Use $tba-api to retrieve or normalize FRC data from The Blue Alliance."

policy:
  allow_implicit_invocation: true
