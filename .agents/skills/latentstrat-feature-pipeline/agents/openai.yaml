interface:
  display_name: "LatentStrat Feature Pipeline"
  short_description: "Build season Parquet and tensor inputs"
  default_prompt: "Use $latentstrat-feature-pipeline to change or review the LatentStrat season feature pipeline."

policy:
  allow_implicit_invocation: true
