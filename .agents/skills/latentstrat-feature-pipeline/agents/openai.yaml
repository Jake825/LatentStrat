interface:
  display_name: "LatentStrat Feature Pipeline"
  short_description: "Build ML-ready Parquet feature tables"
  default_prompt: "Use $latentstrat-feature-pipeline to build LatentStrat feature tables from FRC data into Parquet."

policy:
  allow_implicit_invocation: true
