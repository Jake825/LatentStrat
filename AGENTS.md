# LatentStrat Agent Contract

- Treat `src/`, tests, `README.md`, and active pages linked from `docs/index.md` as current truth. Treat `docs/archive/` as historical context only.
- Use the grouped `latentstrat` CLI and inspect the relevant `--help` output before documenting commands or options.
- For predictions, features, backtests, and evaluation, preserve what was knowable at the decision time; never introduce post-match or post-event leakage.
- Use one repository skill by default. Add a second only when the requested work genuinely crosses a provider, data, model, scouting, or documentation boundary.
- Do not activate skills merely because another skill mentions a neighboring subsystem. Inspect the repository directly unless specialized guidance is actually required.
