name: latentstrat
channels:
  - pytorch
  - conda-forge
dependencies:
  - python=3.11
  - pip
  - pandas
  - numpy
  - scipy
  - pytorch
  - pip:
      - tbapy==1.3.2
      - statbotics==3.0.0
      - requests-cache
      - diskcache
      - pydantic>=2
      - typer
      - pytest
      - vcrpy
      - ruff
