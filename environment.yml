name: latentstrat
channels:
  - pytorch
  - conda-forge
dependencies:
  - python=3.11
  - pip
  - pandas
  - numpy
  - pyarrow
  - scipy
  - scikit-learn
  - tabulate
  - pytorch
  - pip:
      - tbapy==1.3.2
      - statbotics==3.0.0
      - requests-cache
      - diskcache
      - pydantic>=2
      - python-dotenv
      - sqlmodel>=0.0.22
      - typer
      - pytest
      - vcrpy
      - ruff
